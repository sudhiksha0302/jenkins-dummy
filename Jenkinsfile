pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello from Jenkins!'
                echo 'Build completed successfully.'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing completed successfully.'
            }
        }
    }
}
